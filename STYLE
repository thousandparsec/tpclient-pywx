All code is to be indented with TABS.

Constants will be in all upper case. DEBUG_VALUE, PEANUT

Unbound functions are written in all lowercase with underscores between the words.
def word_word():
	pass

Classes are written with an uppercase letter at the start of each word, except the first in multiword class names.
All methods/functions on a class will be in uppercase letter at the start of each word. The first argument will always
be "self". All attributes will be all lowercase.

class Peanut:
	def Eat(self):
		self.size = 1
	
	def EatMore(self):
		self.size = 2
		self.full = 1

class peanutCruncher:
	pass



